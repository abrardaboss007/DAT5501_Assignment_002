version: 2.1

jobs:
  build:
    docker:
      - image: python:3.8
    steps:
      - checkout
      - run:
          name: Install pytest
          command: pip install pytest
      - run:
          name: Run test_fertility_rates_forecasting
          command: pytest test_suite_fertility_code/test_fertility_rates_forecasting.py
          

workflows:
  version: 2
  build:
    jobs:
      - build
